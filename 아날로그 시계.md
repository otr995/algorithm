---
title: 아날로그 시계
platform: programmers
level: lv2
type: bfs-dfs / dp / graph / greedy / implementation / sort / stack-queue
status: solved / retry / failed
date: 2026-05-04
link: https://school.programmers.co.kr/learn/courses/30/lessons/250135
reason: 복습해야하는 이유 
---

# 프로그래머스 [PCCP 기출문제] 3번 / 아날로그 시계

---

## 🧠 1. 첫 접근 (아이디어)
> 문제를 처음 보고 떠올린 생각 / 직관

- 1초를 기준으로 시침, 분침, 초침이 움직이는 각도를 계산하자
- mod 360으로 시계 안 각도를 유지하면서 겹치는 부분을 체크하기

### 착각한 부분
- 1초 단위로 보면 연속된 이벤트를 모두 체크하지 못함 (0.*초 단위 이벤트)

### 문제점
- 1초 기준 계산으로는 모든 경우의 수를 계산하지 못함
- double로 해도 부동소수점 계산에서 오류가 생길 수 있음

### 사고 전환
- 수학적으로 겹치는 규칙?을 구해서 풀기

---

## ❌ 2. 틀린 접근 / 시행착오

### 시도 1

```java
class Solution {
    public int solution(int h1, int m1, int s1, int h2, int m2, int s2) {
        int answer = 0;

        int start = h1 * 3600 + m1 * 60 + s1;
        int end = h2 * 3600 + m2 * 60 + s2;

        double hour = (h1 % 12) * 30 + m1 * 0.5 + s1 * (1.0 / 120);
        double minute = m1 * 6 + s1 * 0.1;
        double second = s1 * 6;

        double eps = 1e-6;

        for (int t = start; t < end; t++) {

            hour += 1.0 / 120;
            minute += 0.1;
            second += 6;

            hour %= 360;
            minute %= 360;
            second %= 360;

            boolean hm = Math.abs(hour - minute) < eps;
            boolean hs = Math.abs(hour - second) < eps;
            boolean ms = Math.abs(minute - second) < eps;

            if (hm) answer++;
            if (hs) answer++;
            if (ms) answer++;

            if (hm && hs && ms) {
                answer -= 2; 
            }
        }

        return answer;
    }
}

```

- ❗ 왜 틀렸는지:
    - 1초 단위 계산으로 연속된 상황에서는 이벤트를 감지하지 못함
    - 1.556초에서 겹치는 일이 발생하면 알람 이벤트를 감지하지 못함..

---

### 시도 2

```java
class Solution {
    public int solution(int h1, int m1, int s1, int h2, int m2, int s2) {
        int answer = 0;

        int start = h1 * 3600 + m1 * 60 + s1;
        int end = h2 * 3600 + m2 * 60 + s2;
        
        return count(end) - count(start);
    }
    
    public int count(int t){
        int m = (59 * t) / (10*360);
        int h = (719 * t) / (360 * 120);
        
        int result = m + h + 2; 
        
        return t > 43200 ? result-2 : result-1;
    }
}
```
- ❗ 왜 틀렸는지:
    - 초침&분침&시침이 모두 겹치는 부분에 대한 중복제거에 대한 이해가 안감..
    - 아직도 왜 틀렸는지 잘 모르겠음

---

## 🔍 3. 핵심 관찰 (문제 제대로 이해하기)

- 중요한 조건:
    - 
- 놓쳤던 부분:
    - 
- 핵심 포인트:
    - 

---

## ✅ 4. 최종 접근 방법

- 전체 전략:
    - 
- 사용한 자료구조:
    - 
- 로직 흐름:
    1. 
    2. 
    3. 

---

## 💻 5. 최종 코드

```java

```

---

## ⏱ 6. 시간 / 공간 복잡도

- 시간 복잡도: O()
- 공간 복잡도: O()

---

## 😵 7. 어려웠던 점

- 
- 

---

## 📚 8. 배운 점 (핵심 정리)

- 
- 

---

## 🔁 9. 다시 풀면 이렇게 한다

> 복습용 한 줄 요약

- 

---

## 📌 한 줄 회고

- 